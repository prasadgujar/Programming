#include <bits/stdc++.h>
using namespace std;

int main()
{
int t;
std::cin>>t;
while(t--)
{
long int  n, d;
std::cin>>n>>d;
std::vector<long long int> arrival();
std::vector<long long int> required();
std::vector<long long int> saddness();
long long int di,ti,si,i;
for( i = 1; i <=n; i++)
{
std::cin>>di>>ti>>si;
arrival.push_back(di);
arrival.push_back(ti);
arrival.push_back(si);
}
std::sort(arrival.begin(),arrival.end());
for( i=1;i<=n;i++)
{
    if(arrival[i]>d)
    {
        while(n--)
        {

        }
    }
}
}
}
